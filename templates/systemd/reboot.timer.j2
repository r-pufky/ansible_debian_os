[Unit]
Description=periodic system reboot timer

[Timer]
OnBootSec={{ os_cfg_reboot | default("1month") }}
RandomizedDelaySec={{ os_cfg_reboot_variance | ansible.builtin.random(start=0, seed=inventory_hostname) }}
Unit=reboot.service

[Install]
WantedBy=timers.target