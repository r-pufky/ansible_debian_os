---
###############################################################################
# Debian Role Options
###############################################################################

os___release: "{{ {} | r_pufky.data.v3(

    _date='2026-03-11',
    _debian='trixie',
  ) }}"

os___app: "{{ {} | r_pufky.data.v3(

    _tmpfs='/tmp',
    _systemd_d='/etc/systemd/system',
    _motd='/etc/update-motd.d/98-reboot-required',
  ) }}"

###############################################################################
# APT Packages
###############################################################################

os___packages:
  - 'rsync'  # ansible.posix.synchronize (skeleton files).
  - 'locales'  # locales management.
